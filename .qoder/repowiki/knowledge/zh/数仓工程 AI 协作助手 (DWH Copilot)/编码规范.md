- SQL 命名统一采用 snake_case，表名遵循 `{层前缀}_{业务过程}_{粒度后缀}` 格式（如 `dwd_trade_order_di`）。
- 所有正式 SQL 脚本必须包含头部注释块，明确标注用途、依赖表、产出目标及调度周期。
- 变更管理强制执行 No Spec, No Change 原则，任何模型或逻辑修改前必须先完成 Spec 文档的定义与确认。
- 维度建模严格区分代理键（_sk）与业务键（_id），并根据业务场景规范应用 SCD1/SCD2 缓慢变化维策略。