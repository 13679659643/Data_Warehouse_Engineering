- **三段式知识结构**：`rules/`（强制约束）、`knowledge/`（经验模式库）与 `changes/`（变更追踪）构成核心逻辑层。
- **代理调度架构**：`.qoder/skills/dwe/SKILL.md` 作为主入口，通过意图识别路由至 `dwh-sql-reviewer`、`dwh-model-reviewer` 等专职子代理。
- **Spec 驱动闭环**：以 `changes/templates/spec.md` 为真理源，强制执行“需求提案 → 任务拆分 → 实施验证 → 归档沉淀”的工程化工作流。
- **自动化追踪**：集成 `version-tracker` 代理，实现 DDL、SQL 及调度变更的结构化日志自动追加与审计。